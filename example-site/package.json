{
  "name": "example-site",
  "version": "1.0.0",
  "description": "Example static site with Tailwind CSS",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./style.css --minify",
    "dev": "tailwindcss -i ./src/input.css -o ./style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}

